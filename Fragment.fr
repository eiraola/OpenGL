#version 330 core
out vec4 FragColor;

in vec3 Normal;  
in vec3 FragPos;  
in vec2 TexCoords;
in vec2 TexCoord;

uniform vec3 ourColor;
uniform sampler2D ourTexture;

void main()
{
     FragColor = texture(ourTexture, TexCoord);
}